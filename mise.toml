# mise config for encryptor_ecto: toolchain only.
#
#   mise install        provision the toolchain below
#
# This repository is not part of the statifier package family, but it adopts
# the family's satellite conventions wholesale (see CLAUDE.md), and the
# toolchain pin is one of them: the same erlang/elixir pair the satellites
# pin - and the same pair `encryptor` pins, which matters more here than
# anywhere else, because this repo builds `encryptor` from source as a git
# dependency rather than fetching a prebuilt Hex package. One toolchain
# across both repos means the vault compiles here exactly as it does in its
# own checkout. It also satisfies mix.exs's `elixir: "~> 1.18"`.
#
# The corpus, spec, and env blocks statifier-ex carries have no counterpart
# here, and neither does its Java entry - nothing in lib/ or test/ needs a
# JRE. No Postgres entry either: this package's tests do not touch a database
# yet, and whether they should is the migrator bead's call, not this file's.

[tools]
erlang = "27.3"
elixir = "1.18.3-otp-27"
